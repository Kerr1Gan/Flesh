<?xml version="1.0" encoding="utf-8"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ecjtu.ninjabrowser">

    <!-- Permission -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- Application -->
    <application
        android:allowBackup="true"
        android:enabled="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true">

        <!-- HolderActivity -->
        <activity
            android:name="com.ecjtu.ninjabrowser.activity.HolderActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:theme="@android:style/Theme.NoDisplay">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="@string/app_scheme_http" />
                <data android:scheme="@string/app_scheme_https" />
            </intent-filter>
        </activity>

        <!-- HolderService -->
        <service
            android:name="com.ecjtu.ninjabrowser.service.HolderService"
            android:icon="@drawable/ic_launcher"
            android:label="@string/app_name" />

        <!-- BrowserActivity -->
        <activity
            android:name="com.ecjtu.ninjabrowser.activity.BrowserActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleInstance"
            android:theme="@style/BrowserActivityTheme"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <!--<action android:name="android.intent.action.MAIN" />-->
                <action android:name="android.intent.action.WEB_SEARCH" />
                <!--<category android:name="android.intent.category.DEFAULT" />-->
                <!--<category android:name="android.intent.category.LAUNCHER" />-->
            </intent-filter>
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>

        <!-- ReadabilityActivity -->
        <activity
            android:name="com.ecjtu.ninjabrowser.activity.ReadabilityActivity"
            android:label="@string/readability_label"
            android:theme="@style/ReadabilityActivityTheme" />

        <!-- SettingActivity -->
        <activity
            android:name="com.ecjtu.ninjabrowser.activity.SettingActivity"
            android:label="@string/setting_label"
            android:theme="@style/SettingActivityTheme" />

        <!-- WhitelistActivity -->
        <activity
            android:name="com.ecjtu.ninjabrowser.activity.WhitelistActivity"
            android:label="@string/whitelist_label"
            android:theme="@style/WhitelistActivityTheme" />

        <!-- TokenActivity -->
        <activity
            android:name="com.ecjtu.ninjabrowser.activity.TokenActivity"
            android:label="@string/token_label"
            android:theme="@style/TokenActivityTheme" />

        <!-- ClearActivity -->
        <activity
            android:name="com.ecjtu.ninjabrowser.activity.ClearActivity"
            android:label="@string/clear_label"
            android:theme="@style/ClearActivityTheme" />

        <!-- ClearService -->
        <service
            android:name="com.ecjtu.ninjabrowser.service.ClearService"
            android:icon="@drawable/ic_launcher"
            android:label="@string/app_name" />

    </application>

</manifest>